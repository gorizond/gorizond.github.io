# Public hostname for both the Docusaurus site and Rancher dashboard
DOMAIN=localhost

# Optional Rancher backend URL (set to enable proxying dashboard/APIs)
RANCHER_UPSTREAM=
# Example: http://localhost:8080

# Host ports published by docker-compose
HTTP_PORT=8080
