replicaCount: 2

image:
  repository: ghcr.io/gorizond/gorizond.github.io
  tag: ""
  pullPolicy: IfNotPresent
  pullSecrets: []

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}

podSecurityContext: {}

securityContext: {}

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: ""
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - host: docs.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: gorizond-docs-tls
      hosts:
        - docs.example.com

env:
  domain: docs.example.com
  rancherUpstream: "" # nginx proxy upstream inside the pod; separate from direct Ingress routing

# Rancher as defaultBackend - catches all paths not matched by Docusaurus.
# When enabled, Docusaurus paths are explicit, everything else goes to Rancher.
rancher:
  enabled: false
  serviceName: rancher
  servicePort: 80
  namespace: ""
  staticPaths:
    # Direct Rancher endpoints that should bypass the Docusaurus frontend.
    - path: /dashboard
      pathType: Prefix
    - path: /v3
      pathType: Prefix
    - path: /v1
      pathType: Prefix
    - path: /k8s
      pathType: Prefix
    - path: /assets/docker-machine-driver-makecloud
      pathType: Prefix
    - path: /assets/rancher-system-agent-amd64
      pathType: Prefix
    - path: /assets/rancher-system-agent-arm64
      pathType: Prefix
    - path: /assets/rancher-system-agent-s390x
      pathType: Prefix
    - path: /assets/rancher-system-agent-arm
      pathType: Prefix
    - path: /assets/system-agent-uninstall.sh
      pathType: Prefix
  # Additional Rancher routes (e.g. specific /assets/* URLs) can be placed here
  # when they need to reach Rancher even though /assets is routed to Docusaurus.
  additionalStaticPaths: []

# Docusaurus static paths (used when rancher.enabled=true).
# These paths go to Docusaurus, everything else goes to Rancher via defaultBackend.
docusaurus:
  staticPaths:
    - path: /
      pathType: Exact
    - path: /ru
      pathType: Prefix
    - path: /assets
      pathType: Prefix
    - path: /img
      pathType: Prefix
    - path: /blog
      pathType: Prefix
    - path: /docs
      pathType: Prefix
    - path: /contacts
      pathType: Prefix
    - path: /search
      pathType: Prefix
    - path: /sitemap.xml
      pathType: Exact
    - path: /robots.txt
      pathType: Exact
    - path: /favicon.ico
      pathType: Exact
    - path: /manifest.json
      pathType: Exact
  # Extend the Docusaurus list with any extra prefixes (e.g. new locales or API stubs).
  additionalStaticPaths: []

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

livenessProbe:
  httpGet:
    path: /
    port: http
  initialDelaySeconds: 10
  periodSeconds: 15
  timeoutSeconds: 5
  failureThreshold: 6
  successThreshold: 1

readinessProbe:
  httpGet:
    path: /
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
  timeoutSeconds: 3
  failureThreshold: 3
  successThreshold: 1

nodeSelector: {}

tolerations: []

affinity: {}

extraNginxConfig: ""
